[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mu-board"
version = "0.2.0"
description = "Mu sessions with scheduled message delivery"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
mub = "muboard.cli:main"

[tool.setuptools]
packages = ["muboard"]
