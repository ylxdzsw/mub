[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mu-board"
version = "0.1.0"
description = "A terminal project manager and serial task queue for Mu"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
mub = "muboard.cli:main"

[tool.setuptools]
packages = ["muboard"]
